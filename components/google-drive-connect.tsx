// Backward-compat re-export. The connect button now lives inside GoogleDriveCard
// which is rendered alongside the other metric cards on /settings/system.
export { GoogleDriveCard as GoogleDriveConnect } from "@/components/google-drive-card";
